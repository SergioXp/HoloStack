name: Smoke Tests (Cross-Platform)

# DESACTIVADO para ahorrar minutos. Corre 'npm run test' localmente antes de subir.
on:
  # push:
  #   branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch: # Solo se puede lanzar manualmente

jobs:
  smoke-test:
    name: Test on Ubuntu (Optimized)
    runs-on: ubuntu-latest
    if: false # Doble seguridad para que no corra

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4
      # ... resto del archivo intacto ...
